<Page x:Class="YouTubeDownloader.Views.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:YouTubeDownloader.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="关于" FontSize="28" FontWeight="Bold" Margin="0,0,0,24"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="24"
                        Margin="0,0,0,16"
                        HorizontalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Source="ms-appx:///Assets/Square44x44Logo.scale-200.png"
                               Width="100"
                               Height="100"
                               Margin="0,0,0,16"/>

                        <TextBlock Text="{Binding AppName}"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>

                        <TextBlock HorizontalAlignment="Center" Margin="0,8,0,0">
                            <Run Text="版本: "/>
                            <Run Text="{Binding Version}"/>
                        </TextBlock>

                        <TextBlock Text="{Binding Description}"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,16,0,0"
                                   MaxWidth="400"/>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="功能特点" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

                        <ItemsControl ItemsSource="{Binding Features}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <TextBlock Text="•" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="版权声明:" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Copyright}"/>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="许可证:" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding License}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <TextBlock Text="本软件仅供学习交流使用，请勿用于任何商业用途。"
                           TextAlignment="Center"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           FontSize="12"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>