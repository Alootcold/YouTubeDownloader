<Page x:Class="YouTubeDownloader.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:YouTubeDownloader.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="设置" FontSize="28" FontWeight="Bold" Margin="0,0,0,24"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="下载设置" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="下载目录:" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding DownloadPath, Mode=TwoWay}"
                                     IsReadOnly="True"
                                     Height="36"/>
                            <Button Grid.Column="2" Content="浏览" Command="{Binding BrowseCommand}" Margin="8,0,0,0" Height="36"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        CornerRadius="8"
                        Padding="16"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="下载引擎管理" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>
                        <TextBlock Text="点击更新按钮手动更新下载引擎，更新完成后会弹出提示窗口。" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Margin="0,0,0,16"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="yt-dlp:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding YtDlpVersion}" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            <ProgressBar Grid.Column="2" Value="{Binding YtDlpProgress}" Maximum="100" Width="100" Margin="8,0" Visibility="{Binding IsUpdatingYtDlp, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Grid.Column="3" Content="更新" Command="{Binding UpdateYtDlpCommand}" Width="80" IsEnabled="{Binding IsUpdatingYtDlp, Converter={StaticResource InverseBoolConverter}}"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="ffmpeg:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding FfmpegVersion}" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            <ProgressBar Grid.Column="2" Value="{Binding FfmpegProgress}" Maximum="100" Width="100" Margin="8,0" Visibility="{Binding IsUpdatingFfmpeg, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Grid.Column="3" Content="更新" Command="{Binding UpdateFfmpegCommand}" Width="80" IsEnabled="{Binding IsUpdatingFfmpeg, Converter={StaticResource InverseBoolConverter}}"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="deno:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding DenoVersion}" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
                            <ProgressBar Grid.Column="2" Value="{Binding DenoProgress}" Maximum="100" Width="100" Margin="8,0" Visibility="{Binding IsUpdatingDeno, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Grid.Column="3" Content="更新" Command="{Binding UpdateDenoCommand}" Width="80" IsEnabled="{Binding IsUpdatingDeno, Converter={StaticResource InverseBoolConverter}}"/>
                        </Grid>

                        <Button Content="更新全部引擎"
                                Command="{Binding UpdateAllCommand}"
                                HorizontalAlignment="Left"
                                Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                           Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>